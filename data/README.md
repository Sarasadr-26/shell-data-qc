# Data notes

Raw and derived data files are not committed to Git. Record here:

- **Source and link:**
- **License and date accessed:**
- **How to obtain it:** `bash run.sh data`
- **What one row represents:**
- **Known issues:** missing values, duplicates, date ranges

Only tiny synthetic samples may be committed, inside `data/sample/`.
